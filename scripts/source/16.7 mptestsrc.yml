description: '16.7 mptestsrc

  Generate various test patterns, as generated by the MPlayer test filter.


  The size of the generated video is fixed, and is 256x256.

  This source is useful in particular for testing encoding features.


  This source accepts the following options:



  Some examples:


  mptestsrc=t=dc_luma


  will generate a "dc_luma" test pattern.'
name: mptestsrc
parameters:
- description: '<dd><p>Specify the frame rate of the sourced video, as the number
    of frames

    generated per second. It has to be a string in the format

    <var>frame_rate_num</var>/<var>frame_rate_den</var>, an integer number, a floating
    point

    number or a valid video frame rate abbreviation. The default value is

    "25".

    </p>

    </dd>'
  name: rate, r
- description: '<dd><p>Set the duration of the sourced video. See

    <a href="ffmpeg-utils.html#time-duration-syntax">(ffmpeg-utils)the Time duration
    section in the ffmpeg-utils(1) manual</a>

    for the accepted syntax.

    </p>

    <p>If not specified, or the expressed duration is negative, the video is

    supposed to be generated forever.

    </p>

    </dd>'
  name: duration, d
- description: '<dd>

    <p>Set the number or the name of the test to perform. Supported tests are:

    </p><dl compact="compact">

    <dt><samp>dc_luma</samp></dt>

    <dt><samp>dc_chroma</samp></dt>

    <dt><samp>freq_luma</samp></dt>

    <dt><samp>freq_chroma</samp></dt>

    <dt><samp>amp_luma</samp></dt>

    <dt><samp>amp_chroma</samp></dt>

    <dt><samp>cbp</samp></dt>

    <dt><samp>mv</samp></dt>

    <dt><samp>ring1</samp></dt>

    <dt><samp>ring2</samp></dt>

    <dt><samp>all</samp></dt>

    <dt><samp>max_frames, m</samp></dt>

    <dd><p>Set the maximum number of frames generated for each test, default value
    is 30.

    </p>

    </dd>

    </dl>

    <p>Default value is "all", which will cycle through the list of all tests.

    </p></dd>'
  name: test, t
ref: https://ffmpeg.org/ffmpeg-filters.html#mptestsrc
source: '<h3 class="section"><a href="#toc-mptestsrc">16.7 mptestsrc</a></h3>


  <p>Generate various test patterns, as generated by the MPlayer test filter.

  </p>

  <p>The size of the generated video is fixed, and is 256x256.

  This source is useful in particular for testing encoding features.

  </p>

  <p>This source accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>rate, r</samp></dt>

  <dd><p>Specify the frame rate of the sourced video, as the number of frames

  generated per second. It has to be a string in the format

  <var>frame_rate_num</var>/<var>frame_rate_den</var>, an integer number, a floating
  point

  number or a valid video frame rate abbreviation. The default value is

  &quot;25&quot;.

  </p>

  </dd>

  <dt><samp>duration, d</samp></dt>

  <dd><p>Set the duration of the sourced video. See

  <a href="ffmpeg-utils.html#time-duration-syntax">(ffmpeg-utils)the Time duration
  section in the ffmpeg-utils(1) manual</a>

  for the accepted syntax.

  </p>

  <p>If not specified, or the expressed duration is negative, the video is

  supposed to be generated forever.

  </p>

  </dd>

  <dt><samp>test, t</samp></dt>

  <dd>

  <p>Set the number or the name of the test to perform. Supported tests are:

  </p><dl compact="compact">

  <dt><samp>dc_luma</samp></dt>

  <dt><samp>dc_chroma</samp></dt>

  <dt><samp>freq_luma</samp></dt>

  <dt><samp>freq_chroma</samp></dt>

  <dt><samp>amp_luma</samp></dt>

  <dt><samp>amp_chroma</samp></dt>

  <dt><samp>cbp</samp></dt>

  <dt><samp>mv</samp></dt>

  <dt><samp>ring1</samp></dt>

  <dt><samp>ring2</samp></dt>

  <dt><samp>all</samp></dt>

  <dt><samp>max_frames, m</samp></dt>

  <dd><p>Set the maximum number of frames generated for each test, default value is
  30.

  </p>

  </dd>

  </dl>


  <p>Default value is &quot;all&quot;, which will cycle through the list of all tests.

  </p></dd>

  </dl>


  <p>Some examples:

  </p><div class="example">

  <pre class="example">mptestsrc=t=dc_luma

  </pre></div>


  <p>will generate a &quot;dc_luma&quot; test pattern.

  </p>

  '
