description: '8.59 astats

  Display time domain statistical information about the audio channels.

  Statistics are calculated and displayed for each audio channel and,

  where applicable, an overall figure is also given.


  It accepts the following option:


  A description of the measure keys follow:'
name: astats
parameters:
- description: '<dd><p>Short window length in seconds, used for peak and trough RMS
    measurement.

    Default is <code>0.05</code> (50 milliseconds). Allowed range is <code>[0 - 10]</code>.

    </p>

    </dd>'
  name: length
- description: '<dd>

    <p>Set metadata injection. All the metadata keys are prefixed with <code>lavfi.astats.X</code>,

    where <code>X</code> is channel number starting from 1 or string <code>Overall</code>.
    Default is

    disabled.

    </p>

    <p>Available keys for each channel are:

    <var>Bit_depth</var>

    <var>Crest_factor</var>

    <var>DC_offset</var>

    <var>Dynamic_range</var>

    <var>Entropy</var>

    <var>Flat_factor</var>

    <var>Max_difference</var>

    <var>Max_level</var>

    <var>Mean_difference</var>

    <var>Min_difference</var>

    <var>Min_level</var>

    <var>Noise_floor</var>

    <var>Noise_floor_count</var>

    <var>Number_of_Infs</var>

    <var>Number_of_NaNs</var>

    <var>Number_of_denormals</var>

    <var>Peak_count</var>

    <var>Abs_Peak_count</var>

    <var>Peak_level</var>

    <var>RMS_difference</var>

    <var>RMS_peak</var>

    <var>RMS_trough</var>

    <var>Zero_crossings</var>

    <var>Zero_crossings_rate</var>

    </p>

    <p>and for <code>Overall</code>:

    <var>Bit_depth</var>

    <var>DC_offset</var>

    <var>Entropy</var>

    <var>Flat_factor</var>

    <var>Max_difference</var>

    <var>Max_level</var>

    <var>Mean_difference</var>

    <var>Min_difference</var>

    <var>Min_level</var>

    <var>Noise_floor</var>

    <var>Noise_floor_count</var>

    <var>Number_of_Infs</var>

    <var>Number_of_NaNs</var>

    <var>Number_of_denormals</var>

    <var>Number_of_samples</var>

    <var>Peak_count</var>

    <var>Abs_Peak_count</var>

    <var>Peak_level</var>

    <var>RMS_difference</var>

    <var>RMS_level</var>

    <var>RMS_peak</var>

    <var>RMS_trough</var>

    </p>

    <p>For example, a full key looks like <code>lavfi.astats.1.DC_offset</code> or

    <code>lavfi.astats.Overall.Peak_count</code>.

    </p>

    <p>Read below for the description of the keys.

    </p>

    </dd>'
  name: metadata
- description: '<dd><p>Set the number of frames over which cumulative stats are calculated
    before

    being reset. Default is disabled.

    </p>

    </dd>'
  name: reset
- description: '<dd><p>Select the parameters which are measured per channel. The metadata
    keys can

    be used as flags, default is <samp>all</samp> which measures everything.

    <samp>none</samp> disables all per channel measurement.

    </p>

    </dd>'
  name: measure_perchannel
- description: '<dd><p>Select the parameters which are measured overall. The metadata
    keys can

    be used as flags, default is <samp>all</samp> which measures everything.

    <samp>none</samp> disables all overall measurement.

    </p>

    </dd>'
  name: measure_overall
ref: https://ffmpeg.org/ffmpeg-filters.html#astats
source: '<h3 class="section"><a href="#toc-astats-1">8.59 astats</a></h3>


  <p>Display time domain statistical information about the audio channels.

  Statistics are calculated and displayed for each audio channel and,

  where applicable, an overall figure is also given.

  </p>

  <p>It accepts the following option:

  </p><dl compact="compact">

  <dt><samp>length</samp></dt>

  <dd><p>Short window length in seconds, used for peak and trough RMS measurement.

  Default is <code>0.05</code> (50 milliseconds). Allowed range is <code>[0 - 10]</code>.

  </p>

  </dd>

  <dt><samp>metadata</samp></dt>

  <dd>

  <p>Set metadata injection. All the metadata keys are prefixed with <code>lavfi.astats.X</code>,

  where <code>X</code> is channel number starting from 1 or string <code>Overall</code>.
  Default is

  disabled.

  </p>

  <p>Available keys for each channel are:

  <var>Bit_depth</var>

  <var>Crest_factor</var>

  <var>DC_offset</var>

  <var>Dynamic_range</var>

  <var>Entropy</var>

  <var>Flat_factor</var>

  <var>Max_difference</var>

  <var>Max_level</var>

  <var>Mean_difference</var>

  <var>Min_difference</var>

  <var>Min_level</var>

  <var>Noise_floor</var>

  <var>Noise_floor_count</var>

  <var>Number_of_Infs</var>

  <var>Number_of_NaNs</var>

  <var>Number_of_denormals</var>

  <var>Peak_count</var>

  <var>Abs_Peak_count</var>

  <var>Peak_level</var>

  <var>RMS_difference</var>

  <var>RMS_peak</var>

  <var>RMS_trough</var>

  <var>Zero_crossings</var>

  <var>Zero_crossings_rate</var>

  </p>

  <p>and for <code>Overall</code>:

  <var>Bit_depth</var>

  <var>DC_offset</var>

  <var>Entropy</var>

  <var>Flat_factor</var>

  <var>Max_difference</var>

  <var>Max_level</var>

  <var>Mean_difference</var>

  <var>Min_difference</var>

  <var>Min_level</var>

  <var>Noise_floor</var>

  <var>Noise_floor_count</var>

  <var>Number_of_Infs</var>

  <var>Number_of_NaNs</var>

  <var>Number_of_denormals</var>

  <var>Number_of_samples</var>

  <var>Peak_count</var>

  <var>Abs_Peak_count</var>

  <var>Peak_level</var>

  <var>RMS_difference</var>

  <var>RMS_level</var>

  <var>RMS_peak</var>

  <var>RMS_trough</var>

  </p>

  <p>For example, a full key looks like <code>lavfi.astats.1.DC_offset</code> or

  <code>lavfi.astats.Overall.Peak_count</code>.

  </p>

  <p>Read below for the description of the keys.

  </p>

  </dd>

  <dt><samp>reset</samp></dt>

  <dd><p>Set the number of frames over which cumulative stats are calculated before

  being reset. Default is disabled.

  </p>

  </dd>

  <dt><samp>measure_perchannel</samp></dt>

  <dd><p>Select the parameters which are measured per channel. The metadata keys can

  be used as flags, default is <samp>all</samp> which measures everything.

  <samp>none</samp> disables all per channel measurement.

  </p>

  </dd>

  <dt><samp>measure_overall</samp></dt>

  <dd><p>Select the parameters which are measured overall. The metadata keys can

  be used as flags, default is <samp>all</samp> which measures everything.

  <samp>none</samp> disables all overall measurement.

  </p>

  </dd>

  </dl>


  <p>A description of the measure keys follow:

  </p>

  <dl compact="compact">

  <dt><samp>none</samp></dt>

  <dd><p>no measures

  </p>

  </dd>

  <dt><samp>all</samp></dt>

  <dd><p>all measures

  </p>

  </dd>

  <dt><samp>Bit_depth</samp></dt>

  <dd><p>overall bit depth of audio, i.e. number of bits used for each sample

  </p>

  </dd>

  <dt><samp>Crest_factor</samp></dt>

  <dd><p>standard ratio of peak to RMS level (note: not in dB)

  </p>

  </dd>

  <dt><samp>DC_offset</samp></dt>

  <dd><p>mean amplitude displacement from zero

  </p>

  </dd>

  <dt><samp>Dynamic_range</samp></dt>

  <dd><p>measured dynamic range of audio in dB

  </p>

  </dd>

  <dt><samp>Entropy</samp></dt>

  <dd><p>entropy measured across whole audio, entropy of value near 1.0 is typically
  measured for white noise

  </p>

  </dd>

  <dt><samp>Flat_factor</samp></dt>

  <dd><p>flatness (i.e. consecutive samples with the same value) of the signal at
  its peak levels

  (i.e. either <var>Min_level</var> or <var>Max_level</var>)

  </p>

  </dd>

  <dt><samp>Max_difference</samp></dt>

  <dd><p>maximal difference between two consecutive samples

  </p>

  </dd>

  <dt><samp>Max_level</samp></dt>

  <dd><p>maximal sample level

  </p>

  </dd>

  <dt><samp>Mean_difference</samp></dt>

  <dd><p>mean difference between two consecutive samples, i.e. the average of each
  difference between two consecutive samples

  </p>

  </dd>

  <dt><samp>Min_difference</samp></dt>

  <dd><p>minimal difference between two consecutive samples

  </p>

  </dd>

  <dt><samp>Min_level</samp></dt>

  <dd><p>minimal sample level

  </p>

  </dd>

  <dt><samp>Noise_floor</samp></dt>

  <dd><p>minimum local peak measured in dBFS over a short window

  </p>

  </dd>

  <dt><samp>Noise_floor_count</samp></dt>

  <dd><p>number of occasions (not the number of samples) that the signal attained

  <var>Noise floor</var>

  </p>

  </dd>

  <dt><samp>Number_of_Infs</samp></dt>

  <dd><p>number of samples with an infinite value

  </p>

  </dd>

  <dt><samp>Number_of_NaNs</samp></dt>

  <dd><p>number of samples with a NaN (not a number) value

  </p>

  </dd>

  <dt><samp>Number_of_denormals</samp></dt>

  <dd><p>number of samples with a subnormal value

  </p>

  </dd>

  <dt><samp>Number_of_samples</samp></dt>

  <dd><p>number of samples

  </p>

  </dd>

  <dt><samp>Peak_count</samp></dt>

  <dd><p>number of occasions (not the number of samples) that the signal attained
  either

  <var>Min_level</var> or <var>Max_level</var>

  </p>

  </dd>

  <dt><samp>Abs_Peak_count</samp></dt>

  <dd><p>number of occasions that the absolute samples taken from the signal attained

  max absolute value of <var>Min_level</var> and <var>Max_level</var>

  </p>

  </dd>

  <dt><samp>Peak_level</samp></dt>

  <dd><p>standard peak level measured in dBFS

  </p>

  </dd>

  <dt><samp>RMS_difference</samp></dt>

  <dd><p>Root Mean Square difference between two consecutive samples

  </p>

  </dd>

  <dt><samp>RMS_level</samp></dt>

  <dd><p>standard RMS level measured in dBFS

  </p>

  </dd>

  <dt><samp>RMS_peak</samp></dt>

  <dt><samp>RMS_trough</samp></dt>

  <dd><p>peak and trough values for RMS level measured over a short window,

  measured in dBFS.

  </p>

  </dd>

  <dt><samp>Zero crossings</samp></dt>

  <dd><p>number of points where the waveform crosses the zero level axis

  </p>

  </dd>

  <dt><samp>Zero crossings rate</samp></dt>

  <dd><p>rate of Zero crossings and number of audio samples

  </p></dd>

  </dl>


  '
