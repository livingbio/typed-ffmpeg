description: '11.214 sab

  Apply Shape Adaptive Blur.


  The filter accepts the following options:



  Each chroma option value, if not explicitly specified, is set to the

  corresponding luma option value.'
name: sab
parameters:
- description: '<dd><p>Set luma blur filter strength, must be a value in range 0.1-4.0,
    default

    value is 1.0. A greater value will result in a more blurred image, and

    in slower processing.

    </p>

    </dd>'
  name: luma_radius, lr
- description: '<dd><p>Set luma pre-filter radius, must be a value in the 0.1-2.0
    range, default

    value is 1.0.

    </p>

    </dd>'
  name: luma_pre_filter_radius, lpfr
- description: '<dd><p>Set luma maximum difference between pixels to still be considered,
    must

    be a value in the 0.1-100.0 range, default value is 1.0.

    </p>

    </dd>'
  name: luma_strength, ls
- description: '<dd><p>Set chroma blur filter strength, must be a value in range -0.9-4.0.
    A

    greater value will result in a more blurred image, and in slower

    processing.

    </p>

    </dd>'
  name: chroma_radius, cr
- description: '<dd><p>Set chroma pre-filter radius, must be a value in the -0.9-2.0
    range.

    </p>

    </dd>'
  name: chroma_pre_filter_radius, cpfr
- description: '<dd><p>Set chroma maximum difference between pixels to still be considered,

    must be a value in the -0.9-100.0 range.

    </p></dd>'
  name: chroma_strength, cs
ref: https://ffmpeg.org/ffmpeg-filters.html#sab
source: '<h3 class="section"><a href="#toc-sab">11.214 sab</a></h3>


  <p>Apply Shape Adaptive Blur.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>luma_radius, lr</samp></dt>

  <dd><p>Set luma blur filter strength, must be a value in range 0.1-4.0, default

  value is 1.0. A greater value will result in a more blurred image, and

  in slower processing.

  </p>

  </dd>

  <dt><samp>luma_pre_filter_radius, lpfr</samp></dt>

  <dd><p>Set luma pre-filter radius, must be a value in the 0.1-2.0 range, default

  value is 1.0.

  </p>

  </dd>

  <dt><samp>luma_strength, ls</samp></dt>

  <dd><p>Set luma maximum difference between pixels to still be considered, must

  be a value in the 0.1-100.0 range, default value is 1.0.

  </p>

  </dd>

  <dt><samp>chroma_radius, cr</samp></dt>

  <dd><p>Set chroma blur filter strength, must be a value in range -0.9-4.0. A

  greater value will result in a more blurred image, and in slower

  processing.

  </p>

  </dd>

  <dt><samp>chroma_pre_filter_radius, cpfr</samp></dt>

  <dd><p>Set chroma pre-filter radius, must be a value in the -0.9-2.0 range.

  </p>

  </dd>

  <dt><samp>chroma_strength, cs</samp></dt>

  <dd><p>Set chroma maximum difference between pixels to still be considered,

  must be a value in the -0.9-100.0 range.

  </p></dd>

  </dl>


  <p>Each chroma option value, if not explicitly specified, is set to the

  corresponding luma option value.

  </p>

  '
