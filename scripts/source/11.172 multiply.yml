description: '11.172 multiply

  Multiply first video stream pixels values with second video stream pixels values.


  The filter accepts the following options:'
name: multiply
parameters:
- description: '<dd><p>Set the scale applied to second video stream. By default is
    <code>1</code>.

    Allowed range is from <code>0</code> to <code>9</code>.

    </p>

    </dd>'
  name: scale
- description: '<dd><p>Set the offset applied to second video stream. By default is
    <code>0.5</code>.

    Allowed range is from <code>-1</code> to <code>1</code>.

    </p>

    </dd>'
  name: offset
- description: '<dd><p>Specify planes from input video stream that will be processed.

    By default all planes are processed.

    </p></dd>'
  name: planes
ref: https://ffmpeg.org/ffmpeg-filters.html#multiply
source: '<h3 class="section"><a href="#toc-multiply">11.172 multiply</a></h3>

  <p>Multiply first video stream pixels values with second video stream pixels values.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>scale</samp></dt>

  <dd><p>Set the scale applied to second video stream. By default is <code>1</code>.

  Allowed range is from <code>0</code> to <code>9</code>.

  </p>

  </dd>

  <dt><samp>offset</samp></dt>

  <dd><p>Set the offset applied to second video stream. By default is <code>0.5</code>.

  Allowed range is from <code>-1</code> to <code>1</code>.

  </p>

  </dd>

  <dt><samp>planes</samp></dt>

  <dd><p>Specify planes from input video stream that will be processed.

  By default all planes are processed.

  </p></dd>

  </dl>


  '
