description: '11.280 vmafmotion

  Obtain the average VMAF motion score of a video.

  It is one of the component metrics of VMAF.


  The obtained average motion score is printed through the logging system.


  The filter accepts the following options:



  Example:


  ffmpeg -i ref.mpg -vf vmafmotion -f null -'
name: vmafmotion
parameters:
- description: '<dd><p>If specified, the filter will use the named file to save the
    motion score of

    each frame with respect to the previous frame.

    When filename equals "-" the data is sent to standard output.

    </p></dd>'
  name: stats_file
ref: https://ffmpeg.org/ffmpeg-filters.html#vmafmotion
source: '<h3 class="section"><a href="#toc-vmafmotion">11.280 vmafmotion</a></h3>


  <p>Obtain the average VMAF motion score of a video.

  It is one of the component metrics of VMAF.

  </p>

  <p>The obtained average motion score is printed through the logging system.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>stats_file</samp></dt>

  <dd><p>If specified, the filter will use the named file to save the motion score
  of

  each frame with respect to the previous frame.

  When filename equals &quot;-&quot; the data is sent to standard output.

  </p></dd>

  </dl>


  <p>Example:

  </p><div class="example">

  <pre class="example">ffmpeg -i ref.mpg -vf vmafmotion -f null -

  </pre></div>


  '
