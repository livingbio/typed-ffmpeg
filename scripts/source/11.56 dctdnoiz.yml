description: '11.56 dctdnoiz

  Denoise frames using 2D DCT (frequency domain filtering).


  This filter is not designed for real time.


  The filter accepts the following options:'
name: dctdnoiz
parameters:
- description: '<dd><p>Set the noise sigma constant.

    </p>

    <p>This <var>sigma</var> defines a hard threshold of <code>3 * sigma</code>; every
    DCT

    coefficient (absolute value) below this threshold with be dropped.

    </p>

    <p>If you need a more advanced filtering, see <samp>expr</samp>.

    </p>

    <p>Default is <code>0</code>.

    </p>

    </dd>'
  name: sigma, s
- description: "<dd><p>Set number overlapping pixels for each block. Since the filter\
    \ can be slow, you\nmay want to reduce this value, at the cost of a less effective\
    \ filter and the\nrisk of various artefacts.\n</p>\n<p>If the overlapping value\
    \ doesn\u2019t permit processing the whole input width or\nheight, a warning will\
    \ be displayed and according borders won\u2019t be denoised.\n</p>\n<p>Default\
    \ value is <var>blocksize</var>-1, which is the best possible setting.\n</p>\n\
    </dd>"
  name: overlap
- description: '<dd><p>Set the coefficient factor expression.

    </p>

    <p>For each coefficient of a DCT block, this expression will be evaluated as a

    multiplier value for the coefficient.

    </p>

    <p>If this is option is set, the <samp>sigma</samp> option will be ignored.

    </p>

    <p>The absolute value of the coefficient can be accessed through the <var>c</var>

    variable.

    </p>

    </dd>'
  name: expr, e
- description: '<dd><p>Set the <var>blocksize</var> using the number of bits. <code>1&lt;&lt;<var>n</var></code>
    defines the

    <var>blocksize</var>, which is the width and height of the processed blocks.

    </p>

    <p>The default value is <var>3</var> (8x8) and can be raised to <var>4</var> for
    a

    <var>blocksize</var> of 16x16. Note that changing this setting has huge consequences

    on the speed processing. Also, a larger block size does not necessarily means
    a

    better de-noising.

    </p></dd>'
  name: n
ref: https://ffmpeg.org/ffmpeg-filters.html#dctdnoiz
source: '<h3 class="section"><a href="#toc-dctdnoiz">11.56 dctdnoiz</a></h3>


  <p>Denoise frames using 2D DCT (frequency domain filtering).

  </p>

  <p>This filter is not designed for real time.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>sigma, s</samp></dt>

  <dd><p>Set the noise sigma constant.

  </p>

  <p>This <var>sigma</var> defines a hard threshold of <code>3 * sigma</code>; every
  DCT

  coefficient (absolute value) below this threshold with be dropped.

  </p>

  <p>If you need a more advanced filtering, see <samp>expr</samp>.

  </p>

  <p>Default is <code>0</code>.

  </p>

  </dd>

  <dt><samp>overlap</samp></dt>

  <dd><p>Set number overlapping pixels for each block. Since the filter can be slow,
  you

  may want to reduce this value, at the cost of a less effective filter and the

  risk of various artefacts.

  </p>

  <p>If the overlapping value doesn&rsquo;t permit processing the whole input width
  or

  height, a warning will be displayed and according borders won&rsquo;t be denoised.

  </p>

  <p>Default value is <var>blocksize</var>-1, which is the best possible setting.

  </p>

  </dd>

  <dt><samp>expr, e</samp></dt>

  <dd><p>Set the coefficient factor expression.

  </p>

  <p>For each coefficient of a DCT block, this expression will be evaluated as a

  multiplier value for the coefficient.

  </p>

  <p>If this is option is set, the <samp>sigma</samp> option will be ignored.

  </p>

  <p>The absolute value of the coefficient can be accessed through the <var>c</var>

  variable.

  </p>

  </dd>

  <dt><samp>n</samp></dt>

  <dd><p>Set the <var>blocksize</var> using the number of bits. <code>1&lt;&lt;<var>n</var></code>
  defines the

  <var>blocksize</var>, which is the width and height of the processed blocks.

  </p>

  <p>The default value is <var>3</var> (8x8) and can be raised to <var>4</var> for
  a

  <var>blocksize</var> of 16x16. Note that changing this setting has huge consequences

  on the speed processing. Also, a larger block size does not necessarily means a

  better de-noising.

  </p></dd>

  </dl>


  '
