description: '9.8 anoisesrc

  Generate a noise audio signal.


  The filter accepts the following options:'
name: anoisesrc
parameters:
- description: '<dd><p>Specify the sample rate. Default value is 48000 Hz.

    </p>

    </dd>'
  name: sample_rate, r
- description: '<dd><p>Specify the amplitude (0.0 - 1.0) of the generated audio stream.
    Default value

    is 1.0.

    </p>

    </dd>'
  name: amplitude, a
- description: '<dd><p>Specify the duration of the generated audio stream. Not specifying
    this option

    results in noise with an infinite length.

    </p>

    </dd>'
  name: duration, d
- description: '<dd><p>Specify the color of noise. Available noise colors are white,
    pink, brown,

    blue, violet and velvet. Default color is white.

    </p>

    </dd>'
  name: color, colour, c
- description: '<dd><p>Specify a value used to seed the PRNG.

    </p>

    </dd>'
  name: seed, s
- description: '<dd><p>Set the number of samples per each output frame, default is
    1024.

    </p>

    </dd>'
  name: nb_samples, n
- description: '<dd><p>Set the density (0.0 - 1.0) for the velvet noise generator,
    default is 0.05.

    </p></dd>'
  name: density
ref: https://ffmpeg.org/ffmpeg-filters.html#anoisesrc
source: '<h3 class="section"><a href="#toc-anoisesrc">9.8 anoisesrc</a></h3>


  <p>Generate a noise audio signal.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>sample_rate, r</samp></dt>

  <dd><p>Specify the sample rate. Default value is 48000 Hz.

  </p>

  </dd>

  <dt><samp>amplitude, a</samp></dt>

  <dd><p>Specify the amplitude (0.0 - 1.0) of the generated audio stream. Default
  value

  is 1.0.

  </p>

  </dd>

  <dt><samp>duration, d</samp></dt>

  <dd><p>Specify the duration of the generated audio stream. Not specifying this option

  results in noise with an infinite length.

  </p>

  </dd>

  <dt><samp>color, colour, c</samp></dt>

  <dd><p>Specify the color of noise. Available noise colors are white, pink, brown,

  blue, violet and velvet. Default color is white.

  </p>

  </dd>

  <dt><samp>seed, s</samp></dt>

  <dd><p>Specify a value used to seed the PRNG.

  </p>

  </dd>

  <dt><samp>nb_samples, n</samp></dt>

  <dd><p>Set the number of samples per each output frame, default is 1024.

  </p>

  </dd>

  <dt><samp>density</samp></dt>

  <dd><p>Set the density (0.0 - 1.0) for the velvet noise generator, default is 0.05.

  </p></dd>

  </dl>


  '
