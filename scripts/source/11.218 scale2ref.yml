description: '11.218 scale2ref

  Scale (resize) the input video, based on a reference video.


  See the scale filter for available options, scale2ref supports the same but

  uses the reference video instead of the main input as basis. scale2ref also

  supports the following additional constants for the w and

  h options:'
name: scale2ref
parameters:
- description: "<dd><p>The main input video\u2019s width and height\n</p>\n</dd>"
  name: main_w
- description: '<dd><p>The same as <var>main_w</var> / <var>main_h</var>

    </p>

    </dd>'
  name: main_h
- description: "<dd><p>The main input video\u2019s sample aspect ratio\n</p>\n</dd>"
  name: main_a
- description: "<dd><p>The main input video\u2019s display aspect ratio. Calculated\
    \ from\n<code>(main_w / main_h) * main_sar</code>.\n</p>\n</dd>"
  name: main_sar
- description: "<dd><p>The main input video\u2019s horizontal and vertical chroma\
    \ subsample values.\nFor example for the pixel format \"yuv422p\" <var>hsub</var>\
    \ is 2 and <var>vsub</var>\nis 1.\n</p>\n</dd>"
  name: main_dar, mdar
- description: '<dd><p>The (sequential) number of the main input frame, starting from
    0.

    Only available with <code>eval=frame</code>.

    </p>

    </dd>'
  name: main_hsub
- description: '<dd><p>The presentation timestamp of the main input frame, expressed
    as a number of

    seconds. Only available with <code>eval=frame</code>.

    </p>

    </dd>'
  name: main_vsub
- description: '<dd><p>The position (byte offset) of the frame in the main input stream,
    or NaN if

    this information is unavailable and/or meaningless (for example in case of synthetic
    video).

    Only available with <code>eval=frame</code>.

    </p></dd>'
  name: main_n
ref: https://ffmpeg.org/ffmpeg-filters.html#scale2ref
source: '<h3 class="section"><a href="#toc-scale2ref">11.218 scale2ref</a></h3>


  <p>Scale (resize) the input video, based on a reference video.

  </p>

  <p>See the scale filter for available options, scale2ref supports the same but

  uses the reference video instead of the main input as basis. scale2ref also

  supports the following additional constants for the <samp>w</samp> and

  <samp>h</samp> options:

  </p>

  <dl compact="compact">

  <dt><var>main_w</var></dt>

  <dt><var>main_h</var></dt>

  <dd><p>The main input video&rsquo;s width and height

  </p>

  </dd>

  <dt><var>main_a</var></dt>

  <dd><p>The same as <var>main_w</var> / <var>main_h</var>

  </p>

  </dd>

  <dt><var>main_sar</var></dt>

  <dd><p>The main input video&rsquo;s sample aspect ratio

  </p>

  </dd>

  <dt><var>main_dar, mdar</var></dt>

  <dd><p>The main input video&rsquo;s display aspect ratio. Calculated from

  <code>(main_w / main_h) * main_sar</code>.

  </p>

  </dd>

  <dt><var>main_hsub</var></dt>

  <dt><var>main_vsub</var></dt>

  <dd><p>The main input video&rsquo;s horizontal and vertical chroma subsample values.

  For example for the pixel format &quot;yuv422p&quot; <var>hsub</var> is 2 and <var>vsub</var>

  is 1.

  </p>

  </dd>

  <dt><var>main_n</var></dt>

  <dd><p>The (sequential) number of the main input frame, starting from 0.

  Only available with <code>eval=frame</code>.

  </p>

  </dd>

  <dt><var>main_t</var></dt>

  <dd><p>The presentation timestamp of the main input frame, expressed as a number
  of

  seconds. Only available with <code>eval=frame</code>.

  </p>

  </dd>

  <dt><var>main_pos</var></dt>

  <dd><p>The position (byte offset) of the frame in the main input stream, or NaN
  if

  this information is unavailable and/or meaningless (for example in case of synthetic
  video).

  Only available with <code>eval=frame</code>.

  </p></dd>

  </dl>


  '
