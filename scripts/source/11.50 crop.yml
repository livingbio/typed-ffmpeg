description: '11.50 crop

  Crop the input video to given dimensions.


  It accepts the following parameters:



  The out_w, out_h, x, y parameters are

  expressions containing the following constants:



  The expression for out_w may depend on the value of out_h,

  and the expression for out_h may depend on out_w, but they

  cannot depend on x and y, as x and y are

  evaluated after out_w and out_h.


  The x and y parameters specify the expressions for the

  position of the top-left corner of the output (non-cropped) area. They

  are evaluated for each frame. If the evaluated value is not valid, it

  is approximated to the nearest valid value.


  The expression for x may depend on y, and the expression

  for y may depend on x.'
name: crop
parameters:
- description: "<dd><p>The width of the output video. It defaults to <code>iw</code>.\n\
    This expression is evaluated only once during the filter\nconfiguration, or when\
    \ the \u2018<samp>w</samp>\u2019 or \u2018<samp>out_w</samp>\u2019 command is\
    \ sent.\n</p>\n</dd>"
  name: w, out_w
- description: "<dd><p>The height of the output video. It defaults to <code>ih</code>.\n\
    This expression is evaluated only once during the filter\nconfiguration, or when\
    \ the \u2018<samp>h</samp>\u2019 or \u2018<samp>out_h</samp>\u2019 command is\
    \ sent.\n</p>\n</dd>"
  name: h, out_h
- description: '<dd><p>The horizontal position, in the input video, of the left edge
    of the output

    video. It defaults to <code>(in_w-out_w)/2</code>.

    This expression is evaluated per-frame.

    </p>

    </dd>'
  name: x
- description: '<dd><p>The vertical position, in the input video, of the top edge
    of the output video.

    It defaults to <code>(in_h-out_h)/2</code>.

    This expression is evaluated per-frame.

    </p>

    </dd>'
  name: y
- description: '<dd><p>If set to 1 will force the output display aspect ratio

    to be the same of the input, by changing the output sample aspect

    ratio. It defaults to 0.

    </p>

    </dd>'
  name: keep_aspect
- description: '<dd><p>Enable exact cropping. If enabled, subsampled videos will be
    cropped at exact

    width/height/x/y as specified and will not be rounded to nearest smaller value.

    It defaults to 0.

    </p></dd>'
  name: exact
ref: https://ffmpeg.org/ffmpeg-filters.html#crop
source: '<h3 class="section"><a href="#toc-crop">11.50 crop</a></h3>


  <p>Crop the input video to given dimensions.

  </p>

  <p>It accepts the following parameters:

  </p>

  <dl compact="compact">

  <dt><samp>w, out_w</samp></dt>

  <dd><p>The width of the output video. It defaults to <code>iw</code>.

  This expression is evaluated only once during the filter

  configuration, or when the &lsquo;<samp>w</samp>&rsquo; or &lsquo;<samp>out_w</samp>&rsquo;
  command is sent.

  </p>

  </dd>

  <dt><samp>h, out_h</samp></dt>

  <dd><p>The height of the output video. It defaults to <code>ih</code>.

  This expression is evaluated only once during the filter

  configuration, or when the &lsquo;<samp>h</samp>&rsquo; or &lsquo;<samp>out_h</samp>&rsquo;
  command is sent.

  </p>

  </dd>

  <dt><samp>x</samp></dt>

  <dd><p>The horizontal position, in the input video, of the left edge of the output

  video. It defaults to <code>(in_w-out_w)/2</code>.

  This expression is evaluated per-frame.

  </p>

  </dd>

  <dt><samp>y</samp></dt>

  <dd><p>The vertical position, in the input video, of the top edge of the output
  video.

  It defaults to <code>(in_h-out_h)/2</code>.

  This expression is evaluated per-frame.

  </p>

  </dd>

  <dt><samp>keep_aspect</samp></dt>

  <dd><p>If set to 1 will force the output display aspect ratio

  to be the same of the input, by changing the output sample aspect

  ratio. It defaults to 0.

  </p>

  </dd>

  <dt><samp>exact</samp></dt>

  <dd><p>Enable exact cropping. If enabled, subsampled videos will be cropped at exact

  width/height/x/y as specified and will not be rounded to nearest smaller value.

  It defaults to 0.

  </p></dd>

  </dl>


  <p>The <var>out_w</var>, <var>out_h</var>, <var>x</var>, <var>y</var> parameters
  are

  expressions containing the following constants:

  </p>

  <dl compact="compact">

  <dt><samp>x</samp></dt>

  <dt><samp>y</samp></dt>

  <dd><p>The computed values for <var>x</var> and <var>y</var>. They are evaluated
  for

  each new frame.

  </p>

  </dd>

  <dt><samp>in_w</samp></dt>

  <dt><samp>in_h</samp></dt>

  <dd><p>The input width and height.

  </p>

  </dd>

  <dt><samp>iw</samp></dt>

  <dt><samp>ih</samp></dt>

  <dd><p>These are the same as <var>in_w</var> and <var>in_h</var>.

  </p>

  </dd>

  <dt><samp>out_w</samp></dt>

  <dt><samp>out_h</samp></dt>

  <dd><p>The output (cropped) width and height.

  </p>

  </dd>

  <dt><samp>ow</samp></dt>

  <dt><samp>oh</samp></dt>

  <dd><p>These are the same as <var>out_w</var> and <var>out_h</var>.

  </p>

  </dd>

  <dt><samp>a</samp></dt>

  <dd><p>same as <var>iw</var> / <var>ih</var>

  </p>

  </dd>

  <dt><samp>sar</samp></dt>

  <dd><p>input sample aspect ratio

  </p>

  </dd>

  <dt><samp>dar</samp></dt>

  <dd><p>input display aspect ratio, it is the same as (<var>iw</var> / <var>ih</var>)
  * <var>sar</var>

  </p>

  </dd>

  <dt><samp>hsub</samp></dt>

  <dt><samp>vsub</samp></dt>

  <dd><p>horizontal and vertical chroma subsample values. For example for the

  pixel format &quot;yuv422p&quot; <var>hsub</var> is 2 and <var>vsub</var> is 1.

  </p>

  </dd>

  <dt><samp>n</samp></dt>

  <dd><p>The number of the input frame, starting from 0.

  </p>

  </dd>

  <dt><samp>pos</samp></dt>

  <dd><p>the position in the file of the input frame, NAN if unknown; deprecated,

  do not use

  </p>

  </dd>

  <dt><samp>t</samp></dt>

  <dd><p>The timestamp expressed in seconds. It&rsquo;s NAN if the input timestamp
  is unknown.

  </p>

  </dd>

  </dl>


  <p>The expression for <var>out_w</var> may depend on the value of <var>out_h</var>,

  and the expression for <var>out_h</var> may depend on <var>out_w</var>, but they

  cannot depend on <var>x</var> and <var>y</var>, as <var>x</var> and <var>y</var>
  are

  evaluated after <var>out_w</var> and <var>out_h</var>.

  </p>

  <p>The <var>x</var> and <var>y</var> parameters specify the expressions for the

  position of the top-left corner of the output (non-cropped) area. They

  are evaluated for each frame. If the evaluated value is not valid, it

  is approximated to the nearest valid value.

  </p>

  <p>The expression for <var>x</var> may depend on <var>y</var>, and the expression

  for <var>y</var> may depend on <var>x</var>.

  </p>

  '
