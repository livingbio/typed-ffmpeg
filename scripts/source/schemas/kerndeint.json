{
  "description": "11.139 kerndeint\nDeinterlace input video by applying Donald Graft\u2019s adaptive kernel\ndeinterling. Work on interlaced parts of a video to produce\nprogressive frames.\n\nThe description of the accepted parameters follows.",
  "json_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
      "map": {
        "default": 0,
        "description": "Paint pixels exceeding the threshold value to white if set to 1. Default is 0.",
        "enum": [
          0,
          1
        ],
        "type": "integer"
      },
      "order": {
        "default": 0,
        "description": "Set the fields order. Swap fields if set to 1, leave fields alone if 0. Default is 0.",
        "enum": [
          0,
          1
        ],
        "type": "integer"
      },
      "sharp": {
        "default": 0,
        "description": "Enable additional sharpening if set to 1. Default is 0.",
        "enum": [
          0,
          1
        ],
        "type": "integer"
      },
      "thresh": {
        "default": 10,
        "description": "Set the threshold which affects the filter's tolerance when determining if a pixel line must be processed. It must be an integer in the range [0,255] and defaults to 10. A value of 0 will result in applying the process on every pixel.",
        "maximum": 255,
        "minimum": 0,
        "type": "integer"
      },
      "twoway": {
        "default": 0,
        "description": "Enable twoway sharpening if set to 1. Default is 0.",
        "enum": [
          0,
          1
        ],
        "type": "integer"
      }
    },
    "title": "kerndeint options",
    "type": "object"
  },
  "name": "kerndeint",
  "parameters": [
    {
      "description": "<dd><p>Set the threshold which affects the filter\u2019s tolerance when\ndetermining if a pixel line must be processed. It must be an integer\nin the range [0,255] and defaults to 10. A value of 0 will result in\napplying the process on every pixels.\n</p>\n</dd>",
      "name": "thresh"
    },
    {
      "description": "<dd><p>Paint pixels exceeding the threshold value to white if set to 1.\nDefault is 0.\n</p>\n</dd>",
      "name": "map"
    },
    {
      "description": "<dd><p>Set the fields order. Swap fields if set to 1, leave fields alone if\n0. Default is 0.\n</p>\n</dd>",
      "name": "order"
    },
    {
      "description": "<dd><p>Enable additional sharpening if set to 1. Default is 0.\n</p>\n</dd>",
      "name": "sharp"
    },
    {
      "description": "<dd><p>Enable twoway sharpening if set to 1. Default is 0.\n</p></dd>",
      "name": "twoway"
    }
  ],
  "ref": "https://ffmpeg.org/ffmpeg-filters.html#kerndeint",
  "source": "<h3 class=\"section\"><a href=\"#toc-kerndeint\">11.139 kerndeint</a></h3>\n\n<p>Deinterlace input video by applying Donald Graft&rsquo;s adaptive kernel\ndeinterling. Work on interlaced parts of a video to produce\nprogressive frames.\n</p>\n<p>The description of the accepted parameters follows.\n</p>\n<dl compact=\"compact\">\n<dt><samp>thresh</samp></dt>\n<dd><p>Set the threshold which affects the filter&rsquo;s tolerance when\ndetermining if a pixel line must be processed. It must be an integer\nin the range [0,255] and defaults to 10. A value of 0 will result in\napplying the process on every pixels.\n</p>\n</dd>\n<dt><samp>map</samp></dt>\n<dd><p>Paint pixels exceeding the threshold value to white if set to 1.\nDefault is 0.\n</p>\n</dd>\n<dt><samp>order</samp></dt>\n<dd><p>Set the fields order. Swap fields if set to 1, leave fields alone if\n0. Default is 0.\n</p>\n</dd>\n<dt><samp>sharp</samp></dt>\n<dd><p>Enable additional sharpening if set to 1. Default is 0.\n</p>\n</dd>\n<dt><samp>twoway</samp></dt>\n<dd><p>Enable twoway sharpening if set to 1. Default is 0.\n</p></dd>\n</dl>\n\n"
}
