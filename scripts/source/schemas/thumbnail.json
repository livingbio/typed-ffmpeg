{
  "description": "11.254 thumbnail\nSelect the most representative frame in a given sequence of consecutive frames.\n\nThe filter accepts the following options:\n\n\nSince the filter keeps track of the whole frames sequence, a bigger n\nvalue will result in a higher memory usage, so a high value is not recommended.",
  "json_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
      "log": {
        "default": "info",
        "description": "Set the log level to display picked frame stats.",
        "type": "string"
      },
      "n": {
        "default": 100,
        "description": "Set the frames batch size to analyze; in a set of n frames, the filter will pick one of them, and then handle the next batch of n frames until the end.",
        "type": "integer"
      }
    },
    "title": "thumbnail options",
    "type": "object"
  },
  "name": "thumbnail",
  "parameters": [
    {
      "description": "<dd><p>Set the frames batch size to analyze; in a set of <var>n</var> frames, the filter\nwill pick one of them, and then handle the next batch of <var>n</var> frames until\nthe end. Default is <code>100</code>.\n</p>\n</dd>",
      "name": "n"
    },
    {
      "description": "<dd><p>Set the log level to display picked frame stats.\nDefault is <code>info</code>.\n</p></dd>",
      "name": "log"
    }
  ],
  "ref": "https://ffmpeg.org/ffmpeg-filters.html#thumbnail",
  "source": "<h3 class=\"section\"><a href=\"#toc-thumbnail\">11.254 thumbnail</a></h3>\n<p>Select the most representative frame in a given sequence of consecutive frames.\n</p>\n<p>The filter accepts the following options:\n</p>\n<dl compact=\"compact\">\n<dt><samp>n</samp></dt>\n<dd><p>Set the frames batch size to analyze; in a set of <var>n</var> frames, the filter\nwill pick one of them, and then handle the next batch of <var>n</var> frames until\nthe end. Default is <code>100</code>.\n</p>\n</dd>\n<dt><samp>log</samp></dt>\n<dd><p>Set the log level to display picked frame stats.\nDefault is <code>info</code>.\n</p></dd>\n</dl>\n\n<p>Since the filter keeps track of the whole frames sequence, a bigger <var>n</var>\nvalue will result in a higher memory usage, so a high value is not recommended.\n</p>\n"
}
