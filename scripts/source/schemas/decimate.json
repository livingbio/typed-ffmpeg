{
  "description": "11.59 decimate\nDrop duplicated frames at regular intervals.\n\nThe filter accepts the following options:",
  "json_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
      "blockx": {
        "default": 32,
        "description": "Set the size of the x-axis blocks used during metric calculations. Must be a power of two.",
        "type": "integer"
      },
      "blocky": {
        "default": 32,
        "description": "Set the size of the y-axis blocks used during metric calculations. Must be a power of two.",
        "type": "integer"
      },
      "chroma": {
        "default": 1,
        "description": "Set whether or not chroma is considered in the metric calculations.",
        "type": "integer"
      },
      "cycle": {
        "default": 5,
        "description": "Set the number of frames from which one will be dropped. Setting this to N means one frame in every batch of N frames will be dropped.",
        "type": "integer"
      },
      "dupthresh": {
        "default": 1.1,
        "description": "Set the threshold for duplicate detection. If the difference metric for a frame is less than or equal to this value, then it is declared as duplicate.",
        "type": "number"
      },
      "mixed": {
        "default": false,
        "description": "Set whether or not the input only partially contains content to be decimated.",
        "type": "boolean"
      },
      "ppsrc": {
        "default": 0,
        "description": "Mark main input as a pre-processed input and activate clean source input stream.",
        "type": "integer"
      },
      "scthresh": {
        "default": 15,
        "description": "Set scene change threshold.",
        "type": "integer"
      }
    },
    "title": "Decimate Filter Options",
    "type": "object"
  },
  "name": "decimate",
  "parameters": [
    {
      "description": "<dd><p>Set the number of frames from which one will be dropped. Setting this to\n<var>N</var> means one frame in every batch of <var>N</var> frames will be dropped.\nDefault is <code>5</code>.\n</p>\n</dd>",
      "name": "cycle"
    },
    {
      "description": "<dd><p>Set the threshold for duplicate detection. If the difference metric for a frame\nis less than or equal to this value, then it is declared as duplicate. Default\nis <code>1.1</code>\n</p>\n</dd>",
      "name": "dupthresh"
    },
    {
      "description": "<dd><p>Set scene change threshold. Default is <code>15</code>.\n</p>\n</dd>",
      "name": "scthresh"
    },
    {
      "description": "<dd><p>Set the size of the x and y-axis blocks used during metric calculations.\nLarger blocks give better noise suppression, but also give worse detection of\nsmall movements. Must be a power of two. Default is <code>32</code>.\n</p>\n</dd>",
      "name": "blockx"
    },
    {
      "description": "<dd><p>Set the size of the x and y-axis blocks used during metric calculations.\nLarger blocks give better noise suppression, but also give worse detection of\nsmall movements. Must be a power of two. Default is <code>32</code>.\n</p>\n</dd>",
      "name": "blocky"
    },
    {
      "description": "<dd><p>Mark main input as a pre-processed input and activate clean source input\nstream. This allows the input to be pre-processed with various filters to help\nthe metrics calculation while keeping the frame selection lossless. When set to\n<code>1</code>, the first stream is for the pre-processed input, and the second\nstream is the clean source from where the kept frames are chosen. Default is\n<code>0</code>.\n</p>\n</dd>",
      "name": "ppsrc"
    },
    {
      "description": "<dd><p>Set whether or not chroma is considered in the metric calculations. Default is\n<code>1</code>.\n</p>\n</dd>",
      "name": "chroma"
    },
    {
      "description": "<dd><p>Set whether or not the input only partially contains content to be decimated.\nDefault is <code>false</code>.\nIf enabled video output stream will be in variable frame rate.\n</p></dd>",
      "name": "mixed"
    }
  ],
  "ref": "https://ffmpeg.org/ffmpeg-filters.html#decimate",
  "source": "<h3 class=\"section\"><a href=\"#toc-decimate-1\">11.59 decimate</a></h3>\n\n<p>Drop duplicated frames at regular intervals.\n</p>\n<p>The filter accepts the following options:\n</p>\n<dl compact=\"compact\">\n<dt><samp>cycle</samp></dt>\n<dd><p>Set the number of frames from which one will be dropped. Setting this to\n<var>N</var> means one frame in every batch of <var>N</var> frames will be dropped.\nDefault is <code>5</code>.\n</p>\n</dd>\n<dt><samp>dupthresh</samp></dt>\n<dd><p>Set the threshold for duplicate detection. If the difference metric for a frame\nis less than or equal to this value, then it is declared as duplicate. Default\nis <code>1.1</code>\n</p>\n</dd>\n<dt><samp>scthresh</samp></dt>\n<dd><p>Set scene change threshold. Default is <code>15</code>.\n</p>\n</dd>\n<dt><samp>blockx</samp></dt>\n<dt><samp>blocky</samp></dt>\n<dd><p>Set the size of the x and y-axis blocks used during metric calculations.\nLarger blocks give better noise suppression, but also give worse detection of\nsmall movements. Must be a power of two. Default is <code>32</code>.\n</p>\n</dd>\n<dt><samp>ppsrc</samp></dt>\n<dd><p>Mark main input as a pre-processed input and activate clean source input\nstream. This allows the input to be pre-processed with various filters to help\nthe metrics calculation while keeping the frame selection lossless. When set to\n<code>1</code>, the first stream is for the pre-processed input, and the second\nstream is the clean source from where the kept frames are chosen. Default is\n<code>0</code>.\n</p>\n</dd>\n<dt><samp>chroma</samp></dt>\n<dd><p>Set whether or not chroma is considered in the metric calculations. Default is\n<code>1</code>.\n</p>\n</dd>\n<dt><samp>mixed</samp></dt>\n<dd><p>Set whether or not the input only partially contains content to be decimated.\nDefault is <code>false</code>.\nIf enabled video output stream will be in variable frame rate.\n</p></dd>\n</dl>\n\n"
}
