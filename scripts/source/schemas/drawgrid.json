{
  "description": "11.77 drawgrid\nDraw a grid on the input image.\n\nIt accepts the following parameters:\n\n\nThe parameters for x, y, w and h and t are expressions containing the\nfollowing constants:",
  "json_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
      "c": {
        "description": "Alias for 'color'.",
        "type": "string"
      },
      "color": {
        "description": "The color of the grid. Can be a color value or 'invert' to use the inverted luma of the video.",
        "type": "string"
      },
      "h": {
        "description": "Alias for 'height'.",
        "type": "string"
      },
      "height": {
        "description": "The expression which specifies the height of the grid cell. If 0, it is interpreted as the input height minus thickness. Defaults to 0.",
        "type": "string"
      },
      "replace": {
        "description": "If the input has alpha, with 1 the painted grid will overwrite the video's color and alpha pixels. Default is 0.",
        "type": "integer"
      },
      "t": {
        "description": "Alias for 'thickness'.",
        "type": "string"
      },
      "thickness": {
        "description": "The expression which sets the thickness of the grid line. Defaults to 1.",
        "type": "string"
      },
      "w": {
        "description": "Alias for 'width'.",
        "type": "string"
      },
      "width": {
        "description": "The expression which specifies the width of the grid cell. If 0, it is interpreted as the input width minus thickness. Defaults to 0.",
        "type": "string"
      },
      "x": {
        "description": "The expression which specifies the x coordinate of some point of grid intersection. Defaults to 0.",
        "type": "string"
      },
      "y": {
        "description": "The expression which specifies the y coordinate of some point of grid intersection. Defaults to 0.",
        "type": "string"
      }
    },
    "required": [],
    "type": "object"
  },
  "name": "drawgrid",
  "parameters": [
    {
      "description": "<dd><p>The expressions which specify the coordinates of some point of grid intersection (meant to configure offset). Both default to 0.\n</p>\n</dd>",
      "name": "x"
    },
    {
      "description": "<dd><p>The expressions which specify the coordinates of some point of grid intersection (meant to configure offset). Both default to 0.\n</p>\n</dd>",
      "name": "y"
    },
    {
      "description": "<dd><p>The expressions which specify the width and height of the grid cell, if 0 they are interpreted as the\ninput width and height, respectively, minus <code>thickness</code>, so image gets\nframed. Default to 0.\n</p>\n</dd>",
      "name": "width, w"
    },
    {
      "description": "<dd><p>The expressions which specify the width and height of the grid cell, if 0 they are interpreted as the\ninput width and height, respectively, minus <code>thickness</code>, so image gets\nframed. Default to 0.\n</p>\n</dd>",
      "name": "height, h"
    },
    {
      "description": "<dd><p>Specify the color of the grid. For the general syntax of this option,\ncheck the <a href=\"ffmpeg-utils.html#color-syntax\">(ffmpeg-utils)\"Color\" section in the ffmpeg-utils manual</a>. If the special\nvalue <code>invert</code> is used, the grid color is the same as the\nvideo with inverted luma.\n</p>\n</dd>",
      "name": "color, c"
    },
    {
      "description": "<dd><p>The expression which sets the thickness of the grid line. Default value is <code>1</code>.\n</p>\n<p>See below for the list of accepted constants.\n</p>\n</dd>",
      "name": "thickness, t"
    },
    {
      "description": "<dd><p>Applicable if the input has alpha. With <code>1</code> the pixels of the painted grid\nwill overwrite the video\u2019s color and alpha pixels.\nDefault is <code>0</code>, which composites the grid onto the input, leaving the video\u2019s alpha intact.\n</p></dd>",
      "name": "replace"
    }
  ],
  "ref": "https://ffmpeg.org/ffmpeg-filters.html#drawgrid",
  "source": "<h3 class=\"section\"><a href=\"#toc-drawgrid\">11.77 drawgrid</a></h3>\n\n<p>Draw a grid on the input image.\n</p>\n<p>It accepts the following parameters:\n</p>\n<dl compact=\"compact\">\n<dt><samp>x</samp></dt>\n<dt><samp>y</samp></dt>\n<dd><p>The expressions which specify the coordinates of some point of grid intersection (meant to configure offset). Both default to 0.\n</p>\n</dd>\n<dt><samp>width, w</samp></dt>\n<dt><samp>height, h</samp></dt>\n<dd><p>The expressions which specify the width and height of the grid cell, if 0 they are interpreted as the\ninput width and height, respectively, minus <code>thickness</code>, so image gets\nframed. Default to 0.\n</p>\n</dd>\n<dt><samp>color, c</samp></dt>\n<dd><p>Specify the color of the grid. For the general syntax of this option,\ncheck the <a href=\"ffmpeg-utils.html#color-syntax\">(ffmpeg-utils)&quot;Color&quot; section in the ffmpeg-utils manual</a>. If the special\nvalue <code>invert</code> is used, the grid color is the same as the\nvideo with inverted luma.\n</p>\n</dd>\n<dt><samp>thickness, t</samp></dt>\n<dd><p>The expression which sets the thickness of the grid line. Default value is <code>1</code>.\n</p>\n<p>See below for the list of accepted constants.\n</p>\n</dd>\n<dt><samp>replace</samp></dt>\n<dd><p>Applicable if the input has alpha. With <code>1</code> the pixels of the painted grid\nwill overwrite the video&rsquo;s color and alpha pixels.\nDefault is <code>0</code>, which composites the grid onto the input, leaving the video&rsquo;s alpha intact.\n</p></dd>\n</dl>\n\n<p>The parameters for <var>x</var>, <var>y</var>, <var>w</var> and <var>h</var> and <var>t</var> are expressions containing the\nfollowing constants:\n</p>\n<dl compact=\"compact\">\n<dt><samp>dar</samp></dt>\n<dd><p>The input display aspect ratio, it is the same as (<var>w</var> / <var>h</var>) * <var>sar</var>.\n</p>\n</dd>\n<dt><samp>hsub</samp></dt>\n<dt><samp>vsub</samp></dt>\n<dd><p>horizontal and vertical chroma subsample values. For example for the\npixel format &quot;yuv422p&quot; <var>hsub</var> is 2 and <var>vsub</var> is 1.\n</p>\n</dd>\n<dt><samp>in_h, ih</samp></dt>\n<dt><samp>in_w, iw</samp></dt>\n<dd><p>The input grid cell width and height.\n</p>\n</dd>\n<dt><samp>sar</samp></dt>\n<dd><p>The input sample aspect ratio.\n</p>\n</dd>\n<dt><samp>x</samp></dt>\n<dt><samp>y</samp></dt>\n<dd><p>The x and y coordinates of some point of grid intersection (meant to configure offset).\n</p>\n</dd>\n<dt><samp>w</samp></dt>\n<dt><samp>h</samp></dt>\n<dd><p>The width and height of the drawn cell.\n</p>\n</dd>\n<dt><samp>t</samp></dt>\n<dd><p>The thickness of the drawn cell.\n</p>\n<p>These constants allow the <var>x</var>, <var>y</var>, <var>w</var>, <var>h</var> and <var>t</var> expressions to refer to\neach other, so you may for example specify <code>y=x/dar</code> or <code>h=w/dar</code>.\n</p>\n</dd>\n</dl>\n\n"
}
