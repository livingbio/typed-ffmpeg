{
  "description": "8.81 dcshift\nApply a DC shift to the audio.\n\nThis can be useful to remove a DC offset (caused perhaps by a hardware problem\nin the recording chain) from the audio. The effect of a DC offset is reduced\nheadroom and hence volume. The astats filter can be used to determine if\na signal has a DC offset.",
  "json_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
      "limitergain": {
        "description": "Optional. It should have a value much less than 1 (e.g. 0.05 or 0.02) and is used to prevent clipping.",
        "exclusiveMaximum": 1,
        "type": "number"
      },
      "shift": {
        "description": "Set the DC shift. Allowed range is [-1, 1]. It indicates the amount to shift the audio.",
        "maximum": 1,
        "minimum": -1,
        "type": "number"
      }
    },
    "required": [
      "shift"
    ],
    "title": "dcshift options",
    "type": "object"
  },
  "name": "dcshift",
  "parameters": [
    {
      "description": "<dd><p>Set the DC shift, allowed range is [-1, 1]. It indicates the amount to shift\nthe audio.\n</p>\n</dd>",
      "name": "shift"
    },
    {
      "description": "<dd><p>Optional. It should have a value much less than 1 (e.g. 0.05 or 0.02) and is\nused to prevent clipping.\n</p></dd>",
      "name": "limitergain"
    }
  ],
  "ref": "https://ffmpeg.org/ffmpeg-filters.html#dcshift",
  "source": "<h3 class=\"section\"><a href=\"#toc-dcshift\">8.81 dcshift</a></h3>\n<p>Apply a DC shift to the audio.\n</p>\n<p>This can be useful to remove a DC offset (caused perhaps by a hardware problem\nin the recording chain) from the audio. The effect of a DC offset is reduced\nheadroom and hence volume. The <a href=\"#astats\">astats</a> filter can be used to determine if\na signal has a DC offset.\n</p>\n<dl compact=\"compact\">\n<dt><samp>shift</samp></dt>\n<dd><p>Set the DC shift, allowed range is [-1, 1]. It indicates the amount to shift\nthe audio.\n</p>\n</dd>\n<dt><samp>limitergain</samp></dt>\n<dd><p>Optional. It should have a value much less than 1 (e.g. 0.05 or 0.02) and is\nused to prevent clipping.\n</p></dd>\n</dl>\n\n"
}
