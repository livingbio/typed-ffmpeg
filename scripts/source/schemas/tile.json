{
  "description": "11.255 tile\nTile several successive frames together.\n\nThe untile filter can do the reverse.\n\nThe filter accepts the following options:",
  "json_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
      "color": {
        "description": "Specify the color of the unused area. For the syntax of this option, check the \"Color\" section in the ffmpeg-utils manual. The default value of color is \"black\".",
        "type": "string"
      },
      "init_padding": {
        "description": "Set the number of frames to initially be empty before displaying first output frame. This controls how soon will one get first output frame. The value must be between 0 and nb_frames - 1. Default is 0.",
        "minimum": 0,
        "type": "integer"
      },
      "layout": {
        "description": "Set the grid size in the form COLUMNSxROWS. Range is up to UINT_MAX cells. Default is 6x5.",
        "type": "string"
      },
      "margin": {
        "description": "Set the outer border margin in pixels. Range is 0 to 1024. Default is 0.",
        "maximum": 1024,
        "minimum": 0,
        "type": "integer"
      },
      "nb_frames": {
        "description": "Set the maximum number of frames to render in the given area. It must be less than or equal to w x h. The default value is 0, meaning all the area will be used.",
        "type": "integer"
      },
      "overlap": {
        "description": "Set the number of frames to overlap when tiling several successive frames together. The value must be between 0 and nb_frames - 1. Default is 0.",
        "minimum": 0,
        "type": "integer"
      },
      "padding": {
        "description": "Set the inner border thickness (i.e. the number of pixels between frames). For more advanced padding options (such as having different values for the edges), refer to the pad video filter. Range is 0 to 1024. Default is 0.",
        "maximum": 1024,
        "minimum": 0,
        "type": "integer"
      }
    },
    "required": [],
    "title": "tile options",
    "type": "object"
  },
  "name": "tile",
  "parameters": [
    {
      "description": "<dd><p>Set the grid size in the form <code>COLUMNSxROWS</code>. Range is up to UINT_MAX cells.\nDefault is <code>6x5</code>.\n</p>\n</dd>",
      "name": "layout"
    },
    {
      "description": "<dd><p>Set the maximum number of frames to render in the given area. It must be less\nthan or equal to <var>w</var>x<var>h</var>. The default value is <code>0</code>, meaning all\nthe area will be used.\n</p>\n</dd>",
      "name": "nb_frames"
    },
    {
      "description": "<dd><p>Set the outer border margin in pixels. Range is 0 to 1024. Default is <code>0</code>.\n</p>\n</dd>",
      "name": "margin"
    },
    {
      "description": "<dd><p>Set the inner border thickness (i.e. the number of pixels between frames). For\nmore advanced padding options (such as having different values for the edges),\nrefer to the pad video filter. Range is 0 to 1024. Default is <code>0</code>.\n</p>\n</dd>",
      "name": "padding"
    },
    {
      "description": "<dd><p>Specify the color of the unused area. For the syntax of this option, check the\n<a href=\"ffmpeg-utils.html#color-syntax\">(ffmpeg-utils)\"Color\" section in the ffmpeg-utils manual</a>.\nThe default value of <var>color</var> is \"black\".\n</p>\n</dd>",
      "name": "color"
    },
    {
      "description": "<dd><p>Set the number of frames to overlap when tiling several successive frames together.\nThe value must be between <code>0</code> and <var>nb_frames - 1</var>. Default is <code>0</code>.\n</p>\n</dd>",
      "name": "overlap"
    },
    {
      "description": "<dd><p>Set the number of frames to initially be empty before displaying first output frame.\nThis controls how soon will one get first output frame.\nThe value must be between <code>0</code> and <var>nb_frames - 1</var>. Default is <code>0</code>.\n</p></dd>",
      "name": "init_padding"
    }
  ],
  "ref": "https://ffmpeg.org/ffmpeg-filters.html#tile",
  "source": "<h3 class=\"section\"><a href=\"#toc-tile-1\">11.255 tile</a></h3>\n\n<p>Tile several successive frames together.\n</p>\n<p>The <a href=\"#untile\">untile</a> filter can do the reverse.\n</p>\n<p>The filter accepts the following options:\n</p>\n<dl compact=\"compact\">\n<dt><samp>layout</samp></dt>\n<dd><p>Set the grid size in the form <code>COLUMNSxROWS</code>. Range is up to UINT_MAX cells.\nDefault is <code>6x5</code>.\n</p>\n</dd>\n<dt><samp>nb_frames</samp></dt>\n<dd><p>Set the maximum number of frames to render in the given area. It must be less\nthan or equal to <var>w</var>x<var>h</var>. The default value is <code>0</code>, meaning all\nthe area will be used.\n</p>\n</dd>\n<dt><samp>margin</samp></dt>\n<dd><p>Set the outer border margin in pixels. Range is 0 to 1024. Default is <code>0</code>.\n</p>\n</dd>\n<dt><samp>padding</samp></dt>\n<dd><p>Set the inner border thickness (i.e. the number of pixels between frames). For\nmore advanced padding options (such as having different values for the edges),\nrefer to the pad video filter. Range is 0 to 1024. Default is <code>0</code>.\n</p>\n</dd>\n<dt><samp>color</samp></dt>\n<dd><p>Specify the color of the unused area. For the syntax of this option, check the\n<a href=\"ffmpeg-utils.html#color-syntax\">(ffmpeg-utils)&quot;Color&quot; section in the ffmpeg-utils manual</a>.\nThe default value of <var>color</var> is &quot;black&quot;.\n</p>\n</dd>\n<dt><samp>overlap</samp></dt>\n<dd><p>Set the number of frames to overlap when tiling several successive frames together.\nThe value must be between <code>0</code> and <var>nb_frames - 1</var>. Default is <code>0</code>.\n</p>\n</dd>\n<dt><samp>init_padding</samp></dt>\n<dd><p>Set the number of frames to initially be empty before displaying first output frame.\nThis controls how soon will one get first output frame.\nThe value must be between <code>0</code> and <var>nb_frames - 1</var>. Default is <code>0</code>.\n</p></dd>\n</dl>\n\n"
}
