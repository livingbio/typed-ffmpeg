{
  "description": "11.258 tmidequalizer\nApply Temporal Midway Video Equalization effect.\n\nMidway Video Equalization adjusts a sequence of video frames to have the same\nhistograms, while maintaining their dynamics as much as possible. It\u2019s\nuseful for e.g. matching exposures from a video frames sequence.\n\nThis filter accepts the following option:",
  "json_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
      "planes": {
        "default": 15,
        "type": "integer"
      },
      "radius": {
        "default": 5,
        "maximum": 127,
        "minimum": 1,
        "type": "integer"
      },
      "sigma": {
        "default": 0.5,
        "minimum": 0,
        "type": "number"
      }
    },
    "required": [],
    "title": "tmidequalizer options",
    "type": "object"
  },
  "name": "tmidequalizer",
  "parameters": [
    {
      "description": "<dd><p>Set filtering radius. Default is <code>5</code>. Allowed range is from 1 to 127.\n</p>\n</dd>",
      "name": "radius"
    },
    {
      "description": "<dd><p>Set filtering sigma. Default is <code>0.5</code>. This controls strength of filtering.\nSetting this option to 0 effectively does nothing.\n</p>\n</dd>",
      "name": "sigma"
    },
    {
      "description": "<dd><p>Set which planes to process. Default is <code>15</code>, which is all available planes.\n</p></dd>",
      "name": "planes"
    }
  ],
  "ref": "https://ffmpeg.org/ffmpeg-filters.html#tmidequalizer",
  "source": "<h3 class=\"section\"><a href=\"#toc-tmidequalizer\">11.258 tmidequalizer</a></h3>\n\n<p>Apply Temporal Midway Video Equalization effect.\n</p>\n<p>Midway Video Equalization adjusts a sequence of video frames to have the same\nhistograms, while maintaining their dynamics as much as possible. It&rsquo;s\nuseful for e.g. matching exposures from a video frames sequence.\n</p>\n<p>This filter accepts the following option:\n</p>\n<dl compact=\"compact\">\n<dt><samp>radius</samp></dt>\n<dd><p>Set filtering radius. Default is <code>5</code>. Allowed range is from 1 to 127.\n</p>\n</dd>\n<dt><samp>sigma</samp></dt>\n<dd><p>Set filtering sigma. Default is <code>0.5</code>. This controls strength of filtering.\nSetting this option to 0 effectively does nothing.\n</p>\n</dd>\n<dt><samp>planes</samp></dt>\n<dd><p>Set which planes to process. Default is <code>15</code>, which is all available planes.\n</p></dd>\n</dl>\n\n"
}
