description: '11.60 deconvolve

  Apply 2D deconvolution of video stream in frequency domain using second stream

  as impulse.


  The filter accepts the following options:



  The deconvolve filter also supports the framesync options.'
name: deconvolve
parameters:
- description: '<dd><p>Set which planes to process.

    </p>

    </dd>'
  name: planes
- description: '<dd><p>Set which impulse video frames will be processed, can be <var>first</var>

    or <var>all</var>. Default is <var>all</var>.

    </p>

    </dd>'
  name: impulse
- description: '<dd><p>Set noise when doing divisions. Default is <var>0.0000001</var>.
    Useful when width

    and height are not same and not power of 2 or if stream prior to convolving

    had noise.

    </p></dd>'
  name: noise
ref: https://ffmpeg.org/ffmpeg-filters.html#deconvolve
source: '<h3 class="section"><a href="#toc-deconvolve">11.60 deconvolve</a></h3>


  <p>Apply 2D deconvolution of video stream in frequency domain using second stream

  as impulse.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>planes</samp></dt>

  <dd><p>Set which planes to process.

  </p>

  </dd>

  <dt><samp>impulse</samp></dt>

  <dd><p>Set which impulse video frames will be processed, can be <var>first</var>

  or <var>all</var>. Default is <var>all</var>.

  </p>

  </dd>

  <dt><samp>noise</samp></dt>

  <dd><p>Set noise when doing divisions. Default is <var>0.0000001</var>. Useful when
  width

  and height are not same and not power of 2 or if stream prior to convolving

  had noise.

  </p></dd>

  </dl>


  <p>The <code>deconvolve</code> filter also supports the <a href="#framesync">framesync</a>
  options.

  </p>

  '
