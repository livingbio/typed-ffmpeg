description: '9.10 sinc

  Generate a sinc kaiser-windowed low-pass, high-pass, band-pass, or band-reject FIR
  coefficients.


  The resulting stream can be used with afir filter for filtering the audio signal.


  The filter accepts the following options:'
name: sinc
parameters:
- description: '<dd><p>Set sample rate, default is 44100.

    </p>

    </dd>'
  name: sample_rate, r
- description: '<dd><p>Set number of samples per each frame. Default is 1024.

    </p>

    </dd>'
  name: nb_samples, n
- description: '<dd><p>Set high-pass frequency. Default is 0.

    </p>

    </dd>'
  name: hp
- description: '<dd><p>Set low-pass frequency. Default is 0.

    If high-pass frequency is lower than low-pass frequency and low-pass frequency

    is higher than 0 then filter will create band-pass filter coefficients,

    otherwise band-reject filter coefficients.

    </p>

    </dd>'
  name: lp
- description: '<dd><p>Set filter phase response. Default is 50. Allowed range is
    from 0 to 100.

    </p>

    </dd>'
  name: phase
- description: '<dd><p>Set Kaiser window beta.

    </p>

    </dd>'
  name: beta
- description: '<dd><p>Set stop-band attenuation. Default is 120dB, allowed range
    is from 40 to 180 dB.

    </p>

    </dd>'
  name: att
- description: '<dd><p>Enable rounding, by default is disabled.

    </p>

    </dd>'
  name: round
- description: '<dd><p>Set number of taps for high-pass filter.

    </p>

    </dd>'
  name: hptaps
- description: '<dd><p>Set number of taps for low-pass filter.

    </p></dd>'
  name: lptaps
ref: https://ffmpeg.org/ffmpeg-filters.html#sinc
source: '<h3 class="section"><a href="#toc-sinc">9.10 sinc</a></h3>


  <p>Generate a sinc kaiser-windowed low-pass, high-pass, band-pass, or band-reject
  FIR coefficients.

  </p>

  <p>The resulting stream can be used with <a href="#afir">afir</a> filter for filtering
  the audio signal.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>sample_rate, r</samp></dt>

  <dd><p>Set sample rate, default is 44100.

  </p>

  </dd>

  <dt><samp>nb_samples, n</samp></dt>

  <dd><p>Set number of samples per each frame. Default is 1024.

  </p>

  </dd>

  <dt><samp>hp</samp></dt>

  <dd><p>Set high-pass frequency. Default is 0.

  </p>

  </dd>

  <dt><samp>lp</samp></dt>

  <dd><p>Set low-pass frequency. Default is 0.

  If high-pass frequency is lower than low-pass frequency and low-pass frequency

  is higher than 0 then filter will create band-pass filter coefficients,

  otherwise band-reject filter coefficients.

  </p>

  </dd>

  <dt><samp>phase</samp></dt>

  <dd><p>Set filter phase response. Default is 50. Allowed range is from 0 to 100.

  </p>

  </dd>

  <dt><samp>beta</samp></dt>

  <dd><p>Set Kaiser window beta.

  </p>

  </dd>

  <dt><samp>att</samp></dt>

  <dd><p>Set stop-band attenuation. Default is 120dB, allowed range is from 40 to
  180 dB.

  </p>

  </dd>

  <dt><samp>round</samp></dt>

  <dd><p>Enable rounding, by default is disabled.

  </p>

  </dd>

  <dt><samp>hptaps</samp></dt>

  <dd><p>Set number of taps for high-pass filter.

  </p>

  </dd>

  <dt><samp>lptaps</samp></dt>

  <dd><p>Set number of taps for low-pass filter.

  </p></dd>

  </dl>


  '
