description: '11.31 colorbalance

  Modify intensity of primary colors (red, green and blue) of input frames.


  The filter allows an input frame to be adjusted in the shadows, midtones or highlights

  regions for the red-cyan, green-magenta or blue-yellow balance.


  A positive adjustment value shifts the balance towards the primary color, a negative

  value towards the complementary color.


  The filter accepts the following options:'
name: colorbalance
parameters:
- description: '<dd><p>Adjust red, green and blue shadows (darkest pixels).

    </p>

    </dd>'
  name: rs
- description: '<dd><p>Adjust red, green and blue shadows (darkest pixels).

    </p>

    </dd>'
  name: gs
- description: '<dd><p>Adjust red, green and blue shadows (darkest pixels).

    </p>

    </dd>'
  name: bs
- description: '<dd><p>Adjust red, green and blue midtones (medium pixels).

    </p>

    </dd>'
  name: rm
- description: '<dd><p>Adjust red, green and blue midtones (medium pixels).

    </p>

    </dd>'
  name: gm
- description: '<dd><p>Adjust red, green and blue midtones (medium pixels).

    </p>

    </dd>'
  name: bm
- description: '<dd><p>Adjust red, green and blue highlights (brightest pixels).

    </p>

    <p>Allowed ranges for options are <code>[-1.0, 1.0]</code>. Defaults are <code>0</code>.

    </p>

    </dd>'
  name: rh
- description: '<dd><p>Adjust red, green and blue highlights (brightest pixels).

    </p>

    <p>Allowed ranges for options are <code>[-1.0, 1.0]</code>. Defaults are <code>0</code>.

    </p>

    </dd>'
  name: gh
- description: '<dd><p>Adjust red, green and blue highlights (brightest pixels).

    </p>

    <p>Allowed ranges for options are <code>[-1.0, 1.0]</code>. Defaults are <code>0</code>.

    </p>

    </dd>'
  name: bh
- description: '<dd><p>Preserve lightness when changing color balance. Default is
    disabled.

    </p></dd>'
  name: pl
ref: https://ffmpeg.org/ffmpeg-filters.html#colorbalance
source: '<h3 class="section"><a href="#toc-colorbalance">11.31 colorbalance</a></h3>

  <p>Modify intensity of primary colors (red, green and blue) of input frames.

  </p>

  <p>The filter allows an input frame to be adjusted in the shadows, midtones or highlights

  regions for the red-cyan, green-magenta or blue-yellow balance.

  </p>

  <p>A positive adjustment value shifts the balance towards the primary color, a negative

  value towards the complementary color.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>rs</samp></dt>

  <dt><samp>gs</samp></dt>

  <dt><samp>bs</samp></dt>

  <dd><p>Adjust red, green and blue shadows (darkest pixels).

  </p>

  </dd>

  <dt><samp>rm</samp></dt>

  <dt><samp>gm</samp></dt>

  <dt><samp>bm</samp></dt>

  <dd><p>Adjust red, green and blue midtones (medium pixels).

  </p>

  </dd>

  <dt><samp>rh</samp></dt>

  <dt><samp>gh</samp></dt>

  <dt><samp>bh</samp></dt>

  <dd><p>Adjust red, green and blue highlights (brightest pixels).

  </p>

  <p>Allowed ranges for options are <code>[-1.0, 1.0]</code>. Defaults are <code>0</code>.

  </p>

  </dd>

  <dt><samp>pl</samp></dt>

  <dd><p>Preserve lightness when changing color balance. Default is disabled.

  </p></dd>

  </dl>


  '
