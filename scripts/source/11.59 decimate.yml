description: '11.59 decimate

  Drop duplicated frames at regular intervals.


  The filter accepts the following options:'
name: decimate
parameters:
- description: '<dd><p>Set the number of frames from which one will be dropped. Setting
    this to

    <var>N</var> means one frame in every batch of <var>N</var> frames will be dropped.

    Default is <code>5</code>.

    </p>

    </dd>'
  name: cycle
- description: '<dd><p>Set the threshold for duplicate detection. If the difference
    metric for a frame

    is less than or equal to this value, then it is declared as duplicate. Default

    is <code>1.1</code>

    </p>

    </dd>'
  name: dupthresh
- description: '<dd><p>Set scene change threshold. Default is <code>15</code>.

    </p>

    </dd>'
  name: scthresh
- description: '<dd><p>Set the size of the x and y-axis blocks used during metric
    calculations.

    Larger blocks give better noise suppression, but also give worse detection of

    small movements. Must be a power of two. Default is <code>32</code>.

    </p>

    </dd>'
  name: blockx
- description: '<dd><p>Set the size of the x and y-axis blocks used during metric
    calculations.

    Larger blocks give better noise suppression, but also give worse detection of

    small movements. Must be a power of two. Default is <code>32</code>.

    </p>

    </dd>'
  name: blocky
- description: '<dd><p>Mark main input as a pre-processed input and activate clean
    source input

    stream. This allows the input to be pre-processed with various filters to help

    the metrics calculation while keeping the frame selection lossless. When set to

    <code>1</code>, the first stream is for the pre-processed input, and the second

    stream is the clean source from where the kept frames are chosen. Default is

    <code>0</code>.

    </p>

    </dd>'
  name: ppsrc
- description: '<dd><p>Set whether or not chroma is considered in the metric calculations.
    Default is

    <code>1</code>.

    </p>

    </dd>'
  name: chroma
- description: '<dd><p>Set whether or not the input only partially contains content
    to be decimated.

    Default is <code>false</code>.

    If enabled video output stream will be in variable frame rate.

    </p></dd>'
  name: mixed
ref: https://ffmpeg.org/ffmpeg-filters.html#decimate
source: '<h3 class="section"><a href="#toc-decimate-1">11.59 decimate</a></h3>


  <p>Drop duplicated frames at regular intervals.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>cycle</samp></dt>

  <dd><p>Set the number of frames from which one will be dropped. Setting this to

  <var>N</var> means one frame in every batch of <var>N</var> frames will be dropped.

  Default is <code>5</code>.

  </p>

  </dd>

  <dt><samp>dupthresh</samp></dt>

  <dd><p>Set the threshold for duplicate detection. If the difference metric for a
  frame

  is less than or equal to this value, then it is declared as duplicate. Default

  is <code>1.1</code>

  </p>

  </dd>

  <dt><samp>scthresh</samp></dt>

  <dd><p>Set scene change threshold. Default is <code>15</code>.

  </p>

  </dd>

  <dt><samp>blockx</samp></dt>

  <dt><samp>blocky</samp></dt>

  <dd><p>Set the size of the x and y-axis blocks used during metric calculations.

  Larger blocks give better noise suppression, but also give worse detection of

  small movements. Must be a power of two. Default is <code>32</code>.

  </p>

  </dd>

  <dt><samp>ppsrc</samp></dt>

  <dd><p>Mark main input as a pre-processed input and activate clean source input

  stream. This allows the input to be pre-processed with various filters to help

  the metrics calculation while keeping the frame selection lossless. When set to

  <code>1</code>, the first stream is for the pre-processed input, and the second

  stream is the clean source from where the kept frames are chosen. Default is

  <code>0</code>.

  </p>

  </dd>

  <dt><samp>chroma</samp></dt>

  <dd><p>Set whether or not chroma is considered in the metric calculations. Default
  is

  <code>1</code>.

  </p>

  </dd>

  <dt><samp>mixed</samp></dt>

  <dd><p>Set whether or not the input only partially contains content to be decimated.

  Default is <code>false</code>.

  If enabled video output stream will be in variable frame rate.

  </p></dd>

  </dl>


  '
