description: '11.155 maskedclamp

  Clamp the first input stream with the second input and third input stream.


  Returns the value of first stream to be between second input

  stream - undershoot and third input stream + overshoot.


  This filter accepts the following options:'
name: maskedclamp
parameters:
- description: '<dd><p>Default value is <code>0</code>.

    </p>

    </dd>'
  name: undershoot
- description: '<dd><p>Default value is <code>0</code>.

    </p>

    </dd>'
  name: overshoot
- description: '<dd><p>Set which planes will be processed as bitmap, unprocessed planes
    will be

    copied from first stream.

    By default value 0xf, all planes will be processed.

    </p></dd>'
  name: planes
ref: https://ffmpeg.org/ffmpeg-filters.html#maskedclamp
source: '<h3 class="section"><a href="#toc-maskedclamp">11.155 maskedclamp</a></h3>


  <p>Clamp the first input stream with the second input and third input stream.

  </p>

  <p>Returns the value of first stream to be between second input

  stream - <code>undershoot</code> and third input stream + <code>overshoot</code>.

  </p>

  <p>This filter accepts the following options:

  </p><dl compact="compact">

  <dt><samp>undershoot</samp></dt>

  <dd><p>Default value is <code>0</code>.

  </p>

  </dd>

  <dt><samp>overshoot</samp></dt>

  <dd><p>Default value is <code>0</code>.

  </p>

  </dd>

  <dt><samp>planes</samp></dt>

  <dd><p>Set which planes will be processed as bitmap, unprocessed planes will be

  copied from first stream.

  By default value 0xf, all planes will be processed.

  </p></dd>

  </dl>


  '
