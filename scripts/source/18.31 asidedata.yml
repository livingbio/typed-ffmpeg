description: '18.31 sidedata, asidedata

  Delete frame side data, or select frames based on it.


  This filter accepts the following options:'
name: asidedata
parameters:
- description: "<dd><p>Set mode of operation of the filter.\n</p>\n<p>Can be one of\
    \ the following:\n</p>\n<dl compact=\"compact\">\n<dt>\u2018<samp>select</samp>\u2019\
    </dt>\n<dd><p>Select every frame with side data of <code>type</code>.\n</p>\n\
    </dd>\n<dt>\u2018<samp>delete</samp>\u2019</dt>\n<dd><p>Delete side data of <code>type</code>.\
    \ If <code>type</code> is not set, delete all side\ndata in the frame.\n</p>\n\
    </dd>\n</dl>\n</dd>"
  name: mode
- description: '<dd><p>Set side data type used with all modes. Must be set for <code>select</code>
    mode. For

    the list of frame side data types, refer to the <code>AVFrameSideDataType</code>
    enum

    in <samp>libavutil/frame.h</samp>. For example, to choose

    <code>AV_FRAME_DATA_PANSCAN</code> side data, you must specify <code>PANSCAN</code>.

    </p>

    </dd>'
  name: type
ref: https://ffmpeg.org/ffmpeg-filters.html#sidedata_002c
source: '<h3 class="section"><a href="#toc-sidedata_002c-asidedata">18.31 sidedata,
  asidedata</a></h3>


  <p>Delete frame side data, or select frames based on it.

  </p>

  <p>This filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>mode</samp></dt>

  <dd><p>Set mode of operation of the filter.

  </p>

  <p>Can be one of the following:

  </p>

  <dl compact="compact">

  <dt>&lsquo;<samp>select</samp>&rsquo;</dt>

  <dd><p>Select every frame with side data of <code>type</code>.

  </p>

  </dd>

  <dt>&lsquo;<samp>delete</samp>&rsquo;</dt>

  <dd><p>Delete side data of <code>type</code>. If <code>type</code> is not set, delete
  all side

  data in the frame.

  </p>

  </dd>

  </dl>


  </dd>

  <dt><samp>type</samp></dt>

  <dd><p>Set side data type used with all modes. Must be set for <code>select</code>
  mode. For

  the list of frame side data types, refer to the <code>AVFrameSideDataType</code>
  enum

  in <samp>libavutil/frame.h</samp>. For example, to choose

  <code>AV_FRAME_DATA_PANSCAN</code> side data, you must specify <code>PANSCAN</code>.

  </p>

  </dd>

  </dl>


  '
