description: '11.271 varblur

  Apply variable blur filter by using 2nd video stream to set blur radius.

  The 2nd stream must have the same dimensions.


  This filter accepts the following options:



  The varblur filter also supports the framesync options.'
name: varblur
parameters:
- description: '<dd><p>Set min allowed radius. Allowed range is from 0 to 254. Default
    is 0.

    </p></dd>'
  name: min_r
- description: '<dd><p>Set max allowed radius. Allowed range is from 1 to 255. Default
    is 8.

    </p></dd>'
  name: max_r
- description: '<dd><p>Set which planes to process. By default, all are used.

    </p></dd>'
  name: planes
ref: https://ffmpeg.org/ffmpeg-filters.html#varblur
source: '<h3 class="section"><a href="#toc-varblur">11.271 varblur</a></h3>

  <p>Apply variable blur filter by using 2nd video stream to set blur radius.

  The 2nd stream must have the same dimensions.

  </p>

  <p>This filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>min_r</samp></dt>

  <dd><p>Set min allowed radius. Allowed range is from 0 to 254. Default is 0.

  </p></dd>

  <dt><samp>max_r</samp></dt>

  <dd><p>Set max allowed radius. Allowed range is from 1 to 255. Default is 8.

  </p></dd>

  <dt><samp>planes</samp></dt>

  <dd><p>Set which planes to process. By default, all are used.

  </p></dd>

  </dl>


  <p>The <code>varblur</code> filter also supports the <a href="#framesync">framesync</a>
  options.

  </p>

  '
