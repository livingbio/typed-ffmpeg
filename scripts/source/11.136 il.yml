description: '11.136 il

  Deinterleave or interleave fields.


  This filter allows one to process interlaced images fields without

  deinterlacing them. Deinterleaving splits the input frame into 2

  fields (so called half pictures). Odd lines are moved to the top

  half of the output image, even lines to the bottom half.

  You can process (filter) them independently and then re-interleave them.


  The filter accepts the following options:'
name: il
parameters:
- description: "<dd><p>Available values for <var>luma_mode</var>, <var>chroma_mode</var>\
    \ and\n<var>alpha_mode</var> are:\n</p>\n<dl compact=\"compact\">\n<dt>\u2018\
    <samp>none</samp>\u2019</dt>\n<dd><p>Do nothing.\n</p>\n</dd>\n<dt>\u2018<samp>deinterleave,\
    \ d</samp>\u2019</dt>\n<dd><p>Deinterleave fields, placing one above the other.\n\
    </p>\n</dd>\n<dt>\u2018<samp>interleave, i</samp>\u2019</dt>\n<dd><p>Interleave\
    \ fields. Reverse the effect of deinterleaving.\n</p></dd>\n</dl>\n<p>Default\
    \ value is <code>none</code>.\n</p>\n</dd>"
  name: luma_mode, l
- description: '<dd><p>Swap luma/chroma/alpha fields. Exchange even &amp; odd lines.
    Default value is <code>0</code>.

    </p></dd>'
  name: chroma_mode, c
ref: https://ffmpeg.org/ffmpeg-filters.html#il
source: '<h3 class="section"><a href="#toc-il">11.136 il</a></h3>


  <p>Deinterleave or interleave fields.

  </p>

  <p>This filter allows one to process interlaced images fields without

  deinterlacing them. Deinterleaving splits the input frame into 2

  fields (so called half pictures). Odd lines are moved to the top

  half of the output image, even lines to the bottom half.

  You can process (filter) them independently and then re-interleave them.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>luma_mode, l</samp></dt>

  <dt><samp>chroma_mode, c</samp></dt>

  <dt><samp>alpha_mode, a</samp></dt>

  <dd><p>Available values for <var>luma_mode</var>, <var>chroma_mode</var> and

  <var>alpha_mode</var> are:

  </p>

  <dl compact="compact">

  <dt>&lsquo;<samp>none</samp>&rsquo;</dt>

  <dd><p>Do nothing.

  </p>

  </dd>

  <dt>&lsquo;<samp>deinterleave, d</samp>&rsquo;</dt>

  <dd><p>Deinterleave fields, placing one above the other.

  </p>

  </dd>

  <dt>&lsquo;<samp>interleave, i</samp>&rsquo;</dt>

  <dd><p>Interleave fields. Reverse the effect of deinterleaving.

  </p></dd>

  </dl>

  <p>Default value is <code>none</code>.

  </p>

  </dd>

  <dt><samp>luma_swap, ls</samp></dt>

  <dt><samp>chroma_swap, cs</samp></dt>

  <dt><samp>alpha_swap, as</samp></dt>

  <dd><p>Swap luma/chroma/alpha fields. Exchange even &amp; odd lines. Default value
  is <code>0</code>.

  </p></dd>

  </dl>


  '
