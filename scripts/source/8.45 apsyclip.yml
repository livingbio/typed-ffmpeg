description: '8.45 apsyclip

  Apply Psychoacoustic clipper to input audio stream.


  The filter accepts the following options:'
name: apsyclip
parameters:
- description: '<dd><p>Set input gain. By default it is 1. Range is [0.015625 - 64].

    </p>

    </dd>'
  name: level_in
- description: '<dd><p>Set output gain. By default it is 1. Range is [0.015625 - 64].

    </p>

    </dd>'
  name: level_out
- description: '<dd><p>Set the clipping start value. Default value is 0dBFS or 1.

    </p>

    </dd>'
  name: clip
- description: '<dd><p>Output only difference samples, useful to hear introduced distortions.

    By default is disabled.

    </p>

    </dd>'
  name: diff
- description: '<dd><p>Set strength of adaptive distortion applied. Default value
    is 0.5.

    Allowed range is from 0 to 1.

    </p>

    </dd>'
  name: adaptive
- description: '<dd><p>Set number of iterations of psychoacoustic clipper.

    Allowed range is from 1 to 20. Default value is 10.

    </p>

    </dd>'
  name: iterations
- description: '<dd><p>Auto level output signal. Default is disabled.

    This normalizes audio back to 0dBFS if enabled.

    </p></dd>'
  name: level
ref: https://ffmpeg.org/ffmpeg-filters.html#apsyclip
source: '<h3 class="section"><a href="#toc-apsyclip">8.45 apsyclip</a></h3>

  <p>Apply Psychoacoustic clipper to input audio stream.

  </p>

  <p>The filter accepts the following options:

  </p>

  <dl compact="compact">

  <dt><samp>level_in</samp></dt>

  <dd><p>Set input gain. By default it is 1. Range is [0.015625 - 64].

  </p>

  </dd>

  <dt><samp>level_out</samp></dt>

  <dd><p>Set output gain. By default it is 1. Range is [0.015625 - 64].

  </p>

  </dd>

  <dt><samp>clip</samp></dt>

  <dd><p>Set the clipping start value. Default value is 0dBFS or 1.

  </p>

  </dd>

  <dt><samp>diff</samp></dt>

  <dd><p>Output only difference samples, useful to hear introduced distortions.

  By default is disabled.

  </p>

  </dd>

  <dt><samp>adaptive</samp></dt>

  <dd><p>Set strength of adaptive distortion applied. Default value is 0.5.

  Allowed range is from 0 to 1.

  </p>

  </dd>

  <dt><samp>iterations</samp></dt>

  <dd><p>Set number of iterations of psychoacoustic clipper.

  Allowed range is from 1 to 20. Default value is 10.

  </p>

  </dd>

  <dt><samp>level</samp></dt>

  <dd><p>Auto level output signal. Default is disabled.

  This normalizes audio back to 0dBFS if enabled.

  </p></dd>

  </dl>


  '
