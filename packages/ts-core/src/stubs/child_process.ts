// Browser stub — the playground generates FFmpeg commands but never runs them.
export const execFileSync = (): never => {
  throw new Error('execFileSync is not available in the browser');
};
export const spawn = (): never => {
  throw new Error('spawn is not available in the browser');
};
