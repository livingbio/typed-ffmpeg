"""snapshot.py has been moved to version packages (depends on DAG)."""
raise ImportError(
    "ffmpeg_core.utils.snapshot depends on DAG and has been moved. "
    "Use: from ffmpeg.utils.snapshot import ..."
)
